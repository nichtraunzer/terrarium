repomix-output.xml
.ruby-version
# ignore all vendor folders in root and subdirectories
*vendor/*
docker/vendor/*
# SBOM and scan artifacts (generated locally, not committed)
artifacts/

# Local bundler config (generated by `bundle config set ...`) — not for commit
docker/.bundle/
